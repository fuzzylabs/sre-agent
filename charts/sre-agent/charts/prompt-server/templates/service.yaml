apiVersion: v1
kind: Service
metadata:
  name: prompt-server-service
  namespace: { { .Values.global.namespace } }
spec:
  selector:
    app: mcp-prompt-server
  ports:
    - protocol: TCP
      port: { { .Values.promptServer.service.port } }
      targetPort: { { .Values.promptServer.service.targetPort } }
  type: ClusterIP
