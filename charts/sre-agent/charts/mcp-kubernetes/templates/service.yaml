apiVersion: v1
kind: Service
metadata:
  name: kubernetes
  namespace: {{ .Values.global.namespace | default "default" }}
spec:
  selector:
    app: mcp-kubernetes
  ports:
    - protocol: TCP
      port: {{ .Values.mcpKubernetes.service.port | default 3001 }}
      targetPort: {{ .Values.mcpKubernetes.service.targetPort | default 3001 }}
  type: "ClusterIP"
